# Qibla feature

Tap **Qibla** on home → `/qibla`.

Uses shared offline location cache (`OfflineLocationService`) and on-device compass.
Bearing toward the Kaaba is pure math — **never online**.

See also: `../prayer/README.md` for shared permission notes.
